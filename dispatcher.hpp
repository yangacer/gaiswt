#ifndef GAISWT_URI_DISPATCHER_HPP_
#define GAISWT_URI_DISPATCHER_HPP_

#include "handler.hpp"
#include <map>

namespace http {

class uri_dispatcher 
{
public:
  uri_dispatcher();
  ~uri_dispatcher();

private:
  //std::map<std::string,
};

} // namespace http

#endif
